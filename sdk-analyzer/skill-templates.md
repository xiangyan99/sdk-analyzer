# Skill Template for Post-Regeneration Customization Guide

## Output

Generate one file: `{package-path}/.github/skills/{package-name}/SKILL.md`

If the directory already exists, inform the user that existing files will be overwritten, then proceed.

---

## Template

````markdown
---
name: {package-name}
description: 'Post-regeneration customization guide for {package-name}. USE WHEN: running tsp-client update, regenerating from TypeSpec, fixing _patch.py breaks, bumping ApiVersion, or adding operation wrappers.'
---

# {package-name} — Post-Regeneration Customization Guide

> **CRITICAL: NEVER edit auto-generated files.** Files inside `_generated/` (or with the header `"Code generated by Microsoft (R) Python Code Generator"`) are overwritten on every regeneration. All customizations MUST go in `_patch.py` or other non-generated files.

`_patch.py` files survive regeneration, but they import from generated modules that **do** change. The verification steps below catch these breaks.

## File Map

> **WARNING:** Files inside `_generated/` are auto-generated and MUST NOT be edited.

### Generated Files (DO NOT EDIT)

| File | Purpose |
|------|---------|
| {path} | {purpose} |

### Customization Files (SAFE TO MODIFY)

| File | Purpose |
|------|---------|
| {path} | {purpose} |

### Import Dependencies

{Table: which customization files import which generated symbols. These are the fragile links.}

| Customization File | Imports From Generated |
|--------------------|----------------------|
| {patch_file} | {symbols} |

## Post-Regeneration Verification

### Step 1: Verify Imports

Check that all `_patch.py` imports from generated modules still resolve:

```bash
{One python -c "from ..." line per key import, from Dimension 3 import map}
```

If any import fails, find the new name in regenerated code and update the `_patch.py` import.

### Step 2: Check ApiVersion

{If ApiVersion enum was detected:}

```bash
python -c "import json; print(json.load(open('_metadata.json'))['apiVersion'])"
grep -A 20 'class ApiVersion' {file}
```

If the new version is missing, add it to the `ApiVersion` enum and update `DEFAULT_VERSION`.

{If no ApiVersion enum — omit this step.}

### Step 3: Run Static Analysis

```bash
cd {package-path}
azpysdk mypy
azpysdk pylint
```

### Step 4: Update Changelog

Add entries to the `## (Unreleased)` section of `CHANGELOG.md` under `### Features Added`, `### Breaking Changes`, or `### Bugs Fixed` as appropriate.

## Customization Patterns

{For each non-empty category found in Dimension 1, include a subsection with real code snippets. Omit categories with no findings.}

### {Category: Client / Model / Operations / Utility}

{Description of the pattern.}

```python
# From {filename}
{real code snippet}
```

**After regeneration, verify:** {specific check for this pattern}

## Async Parity

| Sync File | Async File | Status | Differences |
|-----------|------------|--------|-------------|
| {sync_path} | {async_path} | {equivalent / divergent / missing} | {differences or "none"} |

## Rules

1. **NEVER edit auto-generated files** — all changes go in `_patch.py` or non-generated files.
2. **Maintain async parity** — every sync customization needs an async counterpart in `aio/`.
3. **Verify imports after every regeneration** — run Step 1 above.
4. {SDK-specific rule derived from analysis}
````

---

## Quality Requirements

1. **Real code examples only** — all code blocks must be extracted from the actual SDK package. Never fabricate examples.
2. **Package-specific content** — all descriptions and rules must be specific to the package under analysis.
3. **Omit empty sections cleanly** — remove subsections for dimensions with no findings. **Never omit:** the critical warning, File Map, Post-Regeneration Verification (at minimum Steps 1, 3, 4), Async Parity, Rules.
4. **Import dependency map present** — the File Map must list which generated symbols each customization file imports.
